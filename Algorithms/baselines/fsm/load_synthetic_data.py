import numpy as np
import ../data_reader as reader

class synthetic_data():
    def __init__

    def load_all(file_path, file_name, label_name):
        data = reader.read_data(file_path + file_name)
        label = reader.read_data(file_path + label_name)
        
    def load_frame()


